overwrite: true
schema: "https://graphql.alpha.gateway.br-master.de/graphql"
documents: "graphql/**/*.graphql"
generates:
  ./graphql/fragmentMatcherData.tsx:
    plugins:
      - "fragment-matcher"
  ./schema.json:
    plugins:
      - "introspection"
